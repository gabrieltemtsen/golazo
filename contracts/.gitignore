# dependencies
node_modules

# secrets — never commit real env files (.env.example IS tracked)
.env
.env.local
.env.*.local
*.key
*.pem

# hardhat build output
cache
artifacts
typechain-types
coverage
coverage.json
