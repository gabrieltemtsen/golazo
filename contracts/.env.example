# ── Deploy ──────────────────────────────────────────────────────────────────
DEPLOYER_PRIVATE_KEY=0xYOUR_DEPLOYER_KEY
GNOSIS_RPC_URL=https://rpc.gnosischain.com
CHIADO_RPC_URL=https://rpc.chiadochain.net

# ── GolazoPool constructor ──────────────────────────────────────────────────
# Circles (CRC) ERC-20 used for stakes. On localhost a MockCRC is auto-deployed.
CRC_TOKEN_ADDRESS=0x...
ORACLE_SIGNER_ADDRESS=0x...      # backend wallet that signs match results
FEE_RECIPIENT_ADDRESS=0x...      # wallet / Circles group that receives the rake

# ── Oracle signing (scripts/signResult.ts) ─────────────────────────────────
ORACLE_PRIVATE_KEY=0xYOUR_ORACLE_KEY
GOLAZO_POOL=0x...                # deployed GolazoPool address
CHAIN_ID=100
